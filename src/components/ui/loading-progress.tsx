// src/components/ui/loading-progress.tsx\n'use client';\n\nimport { useEffect, useState } from 'react';\nimport { cn } from '@/lib/utils';\n\ninterface LoadingProgressProps {\n  className?: string;\n  duration?: number;\n}\n\nexport function LoadingProgress({ className, duration = 2000 }: LoadingProgressProps) {\n  const [progress, setProgress] = useState(0);\n\n  useEffect(() => {\n    const timer = setTimeout(() => setProgress(66), 500);\n    return () => clearTimeout(timer);\n  }, []);\n\n  return (\n    <div className={cn('w-full bg-muted rounded-full h-1', className)}>\n      <div\n        className=\"bg-primary h-1 rounded-full transition-all duration-500 ease-out\"\n        style={{ width: `${progress}%` }}\n      />\n    </div>\n  );\n}\n